# Исходники в открытом виде и пароли — НЕ публикуем и НЕ коммитим.
# Восстанавливаются из зашифрованных страниц: node build.mjs decrypt
_src/

# Access-версия (открытый текст для Cloudflare Pages за Access) — не коммитим
_site/

# build deps
node_modules/
package-lock.json

# локальные проверки
scratch-*
*-shot.png
shot-*.png
m-*.png
d-*.png
